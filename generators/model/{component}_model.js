import { Record, List, Map } from 'immutable';

const DEFAULT_ARGS = {
  uid: undefined
};

class {className}Model extends Record(DEFAULT_ARGS) {
  static _name = '{className}::{className}Model';
}

export default {className}Model;
