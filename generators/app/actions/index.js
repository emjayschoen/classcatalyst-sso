// import * as api from '../../api';

const ACTIONS = {
};

// export const list => () => genericAPIActionCreator(
//   api.list{className}s,
//   ACTIONS.LIST_{constName},
//   'Failed to list {titleName}s',
// );

export default ACTIONS;
